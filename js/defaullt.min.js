$(document).ready(function () {

    // Preload
    const sloganEL = document.querySelector('.slogan')
    sloganEL.textContent = 'Marcio Meireles';
    console.log(sloganEL.textContent + " " + sloganEL.textContent.length)

    // Navbar
    const heaLogoEl = document.querySelector('.heabox .label')
    const contactEl = document.querySelector('.heabox .email')

})